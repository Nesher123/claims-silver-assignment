# Copy to .env only if your shell tooling loads dotenv files.
# The pipeline otherwise uses boto3's standard AWS credential chain.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
AWS_DEFAULT_REGION=us-east-1
